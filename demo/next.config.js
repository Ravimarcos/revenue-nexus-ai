/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  // Everything is computed from committed fixtures at build time.
  // No database, no external calls, nothing that can rot.
};
